{
  "name": "luma-ielts-speaking-coach",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A zero-cost, browser-local IELTS Speaking practice coach.",
  "scripts": {
    "dev": "node local-server.mjs",
    "start": "node local-server.mjs",
    "test": "node --test",
    "calibrate": "node tests/calibration-report.mjs",
    "check": "node --check local-server.mjs && node --check src/app.js && node --check src/cloud.js && node --check src/data.js && node --check src/db.js && node --check src/scoring.js && node --check src/speech.js && node --check api/auth.js && node --check api/sync.js"
  },
  "engines": {
    "node": ">=18"
  }
}
