{
  "name": "luma-ielts-speaking-coach",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A zero-cost, browser-local IELTS Speaking practice coach.",
  "scripts": {
    "dev": "node server.mjs",
    "start": "node server.mjs",
    "test": "node --test",
    "calibrate": "node tests/calibration-report.mjs",
    "check": "node --check src/app.js && node --check src/data.js && node --check src/db.js && node --check src/scoring.js && node --check src/speech.js"
  },
  "engines": {
    "node": ">=18"
  }
}
